<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.AITarotReadingApp" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.AITarotReadingApp" parent="Base.Theme.AITarotReadingApp" />
    <style name="customBottomIndicator" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="android:color">@color/secondary</item>
        <item name="android:width">40dp</item>
        <item name="android:height">25dp</item>
        <item name="cornerRadius">10dp</item>
        <item name="cornerFamily">rounded</item>
    </style>
</resources>